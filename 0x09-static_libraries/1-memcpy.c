#include "main.h"
/**
 *_memcpy - The _memcpy() function copies from memory to
 *memory area
 *@dest:area where bytes are copied to
 *@src:area where bytes are copied from
 *n:number of bytes to copy
 *Return: 0 always
 */
char *_memcpy(char *dest, char *src, unsigned int n)
{
return ("\0");
}
